# 「揭棋全随机」模式 — 后端对接文档

> 2026-09-06 新增模式。基于《前端对接指南.md》（`docs/前端对接指南.md`）之上，**只列增量改动**，未提及的接口/字段全部不变。

---

## 0. 结论速览

| 项目 | 说明 |
|---|---|
| 新模式标识 | `mode: "random"`（与 `standard` / `flip` 并列） |
| 核心差异 | 30 枚非将帅棋子**不分颜色全盘洗牌**；暗子归属/吃子/送将按**有效阵营**（暗子→所在半场方，明子→真身颜色）；战利品**全公开**；**翻棋送将立即判负**（翻出敌子攻击己方将帅 → `reason="绝杀"`，见 §3.3） |
| 上帝模式 | **该模式下强制关闭**：不下发 `god:true`、`god-swap` 一律拒绝 |
| 前端是否已改 | 前端已同步完成（暗子占位按行号、选子/送将自动按位置方、战利品公开），后端按本文实现即可联调 |

---

## 1. 房间模块改动

### 1.1 创建房间 `POST /api/room/create`

`mode` 字段枚举扩展：

| mode | 含义 |
|---|---|
| `standard` | 标准模式 |
| `flip` | 揭棋（默认，同色内洗牌） |
| `random` | **揭棋全随机（新增，跨颜色全盘洗牌）** |

请求示例：

```json
{ "mode": "random", "gameTime": 600, "stepTime": 60 }
```

非法 mode 值返回 `4000 参数错误`（或回退为 `flip`，二选一，与现有处理保持一致即可）。

Room 结构、join/ready/leave 全部不变（`mode` 原样透传）。

---

## 2. 发牌算法（服务端，mode=random 时）

### 2.1 规则

- 帅/将 2 枚明置：红帅 `(9,4)`、黑将 `(0,4)`，`hidden=false`
- 其余 **30 枚**：红方 15 枚（车马相仕炮各2 + 兵5，真实 `color:"red"`）+ 黑方 15 枚（真实 `color:"black"`）
- 30 枚**合并为一个池子，Fisher-Yates 洗牌**，随机填入全部 30 个暗子位：
  - 黑方半场：`(0,0..8)` 除 `(0,4)`、`(2,1)`、`(2,7)`、`(3,0/2/4/6/8)`
  - 红方半场：`(9,0..8)` 除 `(9,4)`、`(7,1)`、`(7,7)`、`(6,0/2/4/6/8)`
- 结果：任一暗子位的棋子颜色随机（红方底线的暗子可能是黑棋）

### 2.2 伪代码

```
types = [R,R,N,N,B,B,A,A,C,C,P,P,P,P,P]        # 红15
pieces = types.map(t => ({t, color:"red"}))
          .concat(types.map(t => ({t, color:"black"})))
shuffle(pieces)                                  # Fisher-Yates
squares = 全部30个暗子位（坐标列表）
squares[i] <- pieces[i]，全部 hidden=true
```

### 2.3 State 下发（random 模式保密升级）

- 对**所有玩家**（包括昵称"炫"的玩家）：暗子一律 `{ "t": "?", "color": "<位置方颜色>", "hidden": true }`
- **`color` 必须掩码为"位置方颜色"**：row≥5（红半场）→ `"red"`，row≤4（黑半场）→ `"black"`。
  真身颜色**绝不下发**——否则渲染层会直接泄露每枚暗子的阵营，且与"暗子归属跟半场"矛盾
- **`mode=random` 时任何响应/推送都不得携带 `god: true`**，暗子 `t` 绝不下发真身
- 战利品（capturedTray）在 random 模式下**全公开**，见 §4

---

## 3. 走法校验规则（服务端引擎同步）

服务端复用 `xiangqi.js`：**整体替换为前端版本**即可（`createRandomFlipGame` / 位置行为逻辑都在其中）。所有走法/将军/终局判定一律在**掩码盘**上执行（见 3.0），引擎本身零改动。若服务端自行实现，必须满足：

### 3.0 掩码盘校验（推荐实现，天然无歧义）

引擎函数（`pieceMoves` / `legalMoves` / `isInCheck` / `hasLegalMove` / `gameStatus`）不跑在真实棋盘上，而是跑在"掩码盘"上：

```
maskBoard(board, hidden)：
  暗子（hidden[r][c]=true）→ 占位子：row>=5 → 'Q'（红），row<=4 → 'q'（黑）
  明子 → 原样保留（真身大小写不变）
```

- 掩码盘上暗子颜色天然 = 位置方，**归属、吃子权限、送将保护自动全部正确**，且与前端线上渲染表示（Q/q 占位）完全一致
- 真实棋盘仅用于：真身记录、走子后翻开（hidden=false）、吃子入罐（真实 t/color）、终局前最后一步渲染
- 路由层权限校验同口径：请求方只能移动掩码盘上 `isRed(p) === (mySide==='red')` 的棋子

### 3.1 暗子行为 = 位置行为；归属/吃子/送将 = 有效阵营（本模式核心）

未翻开的暗子按"该位置标准棋子"行动，但方向类限制**以位置所在半场为准，与棋子真实颜色无关**：

| 行为项 | 判定依据 |
|---|---|
| 走法类型（车/马/炮…） | 位置的标准类型（`standardTypeAt`，上下对称与颜色无关，不变） |
| 兵/卒前进方向 | **位置方**：红半场（row≥5）的暗子兵行为 = 向 row 减小方向拱；黑半场 = 向 row 增大 |
| 士行为九宫范围 | **位置方**的九宫 |
| 相行为河界限制 | **位置方**：黑半场暗子相行为不过 row≥5，红半场不过 row≤4 |
| **归属 / 吃子权限** | **有效阵营**：暗子→所在半场方（row≥5=红），明子→真身颜色。只能吃有效阵营为对方的子——我方半场暗子**可吃对方半场暗子（不论双方真身颜色）**；同半场暗子不可互吃；不能吃己方有效阵营的明子 |
| **送将保护** | 走子子的**有效阵营**一方的将（红半场暗子被红方走时只保护红帅 K；掩码盘上自动成立） |
| 蹩马腿/塞象眼 | 只看位置，无方向性，不变 |

> 对照：`flip` 模式下棋子颜色恒等于位置方，因此"方向跟位置"与"方向跟颜色"结果相同，**该改动对 flip 模式零影响**，只是让 random 模式语义正确。

### 3.2 其余规则与 flip 完全一致

- 暗子**不构成攻击**（不将军），但占位挡线、挡炮架
- 翻开后按真身+真实颜色行棋（黑卒过河后横走、真身士任意斜走、真身相可过河田字——与 flip 模式相同的解限规则）
- 翻开的真身士/相**不受九宫/河界限制**（沿用现有自定义规则）
- 将帅照面、送将、绝杀/困毙、超时判负全部不变
- `reason` 枚举不变：`绝杀 / 困毙 / 认输 / 超时`

### 3.3 落子后判定切回真盘：翻棋送将 → 立即判负（本模式特有）

掩码盘只负责**落子前**的合法性校验；走子应用并翻开真身后，一切将军/终局判定必须切回**真实棋盘**：

1. **翻棋送将（立即判负）**：翻开的真身可能是对方的棋（当场易主）。掩码盘校验时它是己方 Q/q（合法），真盘上它是敌子——若这枚敌子在其**落点**直接攻击行棋方自己的将帅（`isInCheck(realBoard, moverColor, hidden, flipMode) === true`），行棋方必败（对方下一手直接吃将，无应对机会）：
   - 走子响应 State 直接 `status:"ended"`、`winner = 对方`、`reason = "绝杀"`
   - 同时向双方推送 `game_over`（走子方收到 ended State + 推送，前端 ending 门闩防重）
   - 无需等对方真的走一手吃将
2. **check 字段**：走子响应/推送中的 `check` 按真盘算（翻开的子按真身、真实颜色参与攻击）
3. **将死/困毙**：`gameStatus` 在真盘上跑。特别注意翻开的敌子**归对方所有、对方可以走它**——`hasLegalMove` 必须把它计入对方着法，否则会误判困毙
4. flip 模式下真身恒为己方棋子，规则 1 天然不触发；standard 无翻棋。判定逻辑可无条件实现（每步走子后都做真盘检查），对 flip/standard 零影响

---

## 4. 战利品全公开（random 模式新要求）

| 项 | flip 模式（照旧） | random 模式（新要求） |
|---|---|---|
| 吃到明子 | 双方可见真身 | 双方可见真身 |
| 吃到暗子 | 仅吃方见真身；对非吃方 `capturedTray` 下发 `t:"?"` | **双方均下发真实 `t` + `color`** |
| 前端展示 | 对方盘上暗子捕获显示"？"（Q/q 占位） | 双方盘上均显示真实棋子名，同类型叠加角标计数 |

- **归属**：战利品一律记入"走子方玩家"的盘子，与棋子真身颜色无关（红半场暗子被红方走并吃子 → 记入红方盘子）
- 服务端改动点：构造 `capturedTray` 时若 `mode=random`，跳过"对非吃方掩码暗子真身"分支，直接下发真实 `t`/`color`
- 前端已同步：`capTraysFor` 在 random 模式下不再对对方盘做本地掩码
- move 响应中的 `captured`（吃暗子提示）字段照旧，仅吃方收到

## 5. 上帝模式在 random 下的关闭

| 项 | 要求 |
|---|---|
| 登录 `god:true` | 用户级标记照常返回（不影响其他模式） |
| State 下发 | `mode=random` 时**不携带 `god` 字段**，暗子 `t` 对所有人都是 `"?"` |
| `POST /api/game/:roomId/god-swap` | `mode=random` 时返回 `4000`（msg 任意，如"该模式不支持"），**不暴露功能存在** |

---

## 6. 验收清单

**发牌与掩码**
- [ ] `create {mode:"random"}` 建房成功，Room.mode = "random"
- [ ] 双方 state：帅/将明置且颜色正确；30 个暗子 `t:"?"` 且 `color` = 位置方颜色（红半场 `"red"`），真身颜色不下发
- [ ] 统计校验：暗子真实颜色红黑各 15（服务端内部自测；客户端不可见）

**走法**
- [ ] 红半场暗子（真身黑卒）按红兵方向前进（向 row 减小），过河后可横移
- [ ] 黑半场暗子（真身红马）可被**黑方玩家**移动，蹩马腿正常
- [ ] 红方玩家可移动红半场全部暗子（含真身为黑的）；无法移动黑半场暗子（含真身为红的）；翻开后按真身颜色归属
- [ ] 跨半场暗子可互吃（不论双方真身颜色）；同半场暗子不可互吃；暗子不能吃己方有效阵营的明子
- [ ] 红半场暗子被红方移动时送将保护红帅（走子后红帅被将 → 拒绝 4003）
- [ ] 翻开后按真身+真色走子；真身士/相解限生效

**上帝模式关闭**
- [ ] 昵称"炫"进 random 模式：state 无 `god` 字段，暗子全 `?`
- [ ] 昵称"炫"在 random 模式调 god-swap → 4000
- [ ] 同一"炫"用户进 flip 模式：上帝模式一切照旧（不受影响）

**战利品公开（random）**
- [ ] 任一方吃掉暗子后，双方 capturedTray 均含真实 t/color，前端显示真名真色
- [ ] 战利品记入走子方盘子（与被吃棋子真身颜色无关）
- [ ] flip 模式不受影响：对方盘上暗子捕获仍显示"？"

**翻棋送将（落子后真盘判定）**
- [ ] 仕位暗子翻出敌兵且落点攻击己方将帅 → 走子响应直接 `status:"ended"`、`winner=对方`、`reason="绝杀"`，双方收到 game_over
- [ ] 翻出敌子但未攻击将帅 → 对局继续，`check` 按真盘计算
- [ ] 翻开的敌子归对方后，对方可以走这枚子（`hasLegalMove` 计入，不误判困毙）
- [ ] flip 模式回归：无翻棋送将场景，走法/终局判定不受影响

**对局闭环**
- [ ] 绝杀/困毙 reason 正常推送，认输/超时/悔棋/再来一局照常
- [ ] 再来一局（rematch）重新全盘洗牌
