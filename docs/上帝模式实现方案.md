# 上帝模式（God Mode）实现方案

> 触发条件：玩家昵称为 **"炫"**（精确匹配，去空格）。
> 性质：**仅该玩家客户端可见的调试/娱乐功能**，另一名玩家数据、界面、推送完全无感知。

---

## 0. 结论速览

| 问题 | 结论 |
|---|---|
| 后端是否要改？ | **必须改**。暗棋真身只存在服务端，透视数据与换身份操作都在服务端 |
| 前端是否要改？ | 要改（透视渲染 + 长按弹窗 + 新接口调用），见 §3 |
| 对方能否察觉？ | 不能。对方收到的 State / WS 推送与平时逐字节一致 |
| 是否影响规则引擎？ | 不影响。交换只改服务端发牌数据，走子/将军/判定全部照旧自动生效 |

---

## 1. 需求拆解

1. **开启**：输入名字 === "炫" → 该用户标记为上帝（god）。
2. **隐蔽性**：上帝界面只在上帝自己的手机上出现；对方客户端完全正常，且**无法通过任何数据/请求/推送推断**上帝模式存在。
3. **透视**：
   - 上帝客户端能看到所有暗棋的真实身份；
   - 暗棋（已透视）样式压暗一档（如 opacity 0.7 + 亮度降低），明棋正常显示；
   - 纯视觉，**不改变任何游戏规则**：暗棋未翻开前仍按"所在位置"的走法行动（揭棋规则不变）。
4. **换身份**：
   - **长按任意一枚暗棋** → 弹出类型选择框，可把该暗棋更换为其他类型；
   - **将帅（K/k）不可更换**；
   - **守恒约束**：每种棋子总数固定（车2、马2、相2、仕2、炮2、兵5），"更换"本质是**两枚暗棋交换真身**：
     - 例：长按的暗棋真身是车，想换成马 → 服务端在同色、仍存活的暗棋中随机找一枚真身是马的，两枚互换（车变马、马变车），总数不变；
     - 目标类型在棋盘上**已没有暗棋**（全部被吃或全部已翻开）→ 不可选（否则等于凭空造棋）；
   - 已翻开的明棋、已被吃掉的棋子、将帅：不参与交换。

---

## 2. 后端改动清单（必须）

### 2.1 上帝标记（用户级）

- 登录接口 `POST /api/user/login`：`nickname` 去除首尾空格后 **=== "炫"** → 该用户 `god = true`，存入用户记录/token 会话。
- 登录成功响应增加字段：

```json
{ "code": 0, "data": { "token": "...", "userId": "...", "nickname": "炫", "avatarUrl": "...", "god": true } }
```

- `GET /api/user/me` 同样返回 `god` 字段。
- 普通用户返回 `"god": false` 或不返回该字段。

> 说明：这是娱乐功能，按昵称判定即需求本身；若以后要严格控制，可改成 userId 白名单。

### 2.2 State 下发：上帝视角（仅上帝可见）

在所有给**上帝玩家**返回局面的地方（`GET /api/game/:roomId/state`、WS `game_start`、WS `move_applied`），State 增加：

```json
{
  "god": true,
  "board": [
    [ { "t": "R", "color": "black", "hidden": true }, null, "..." ]
  ]
}
```

- **上帝客户端**：暗棋棋子的 `t` 下发**真实类型**（大写 `K/A/B/N/R/C/P`），但 `hidden` 仍为 `true`；
- **普通客户端（含对手）**：暗棋保持 `{ "t": "?", "color": "...", "hidden": true }`，**逐字节不变**；
- 额外下发顶层 `god: true`（仅上帝有此字段），便于前端显式开启上帝 UI（不靠猜 t 来判断）。

**保密红线（后端务必自查）**：
- 序列化 State 的函数必须按"当前连接/请求所属玩家"分别裁剪；
- 对手的 HTTP 响应、WS 推送中**绝不能**出现真实 t 或 `god` 字段；
- 上帝换身份不产生任何面向对方的推送（对方可见状态没有任何变化）。

### 2.3 新增接口：上帝换身份

`POST /api/game/:roomId/god-swap`

**请求：**

```json
{ "from": [7, 2], "toType": "N" }
```

| 字段 | 说明 |
|---|---|
| from | 要更换的暗棋坐标 [row, col] |
| toType | 目标类型，大写 `A/B/N/R/C/P`（不含 K，P=兵/卒） |

**服务端逻辑：**

1. 校验调用者 `god === true`，否则 `4000 无权限`（对普通用户表现为普通业务错误，不暴露该功能）；
2. 取 `from` 位置棋子：必须存在、`hidden === true`（未翻开）、真实类型 ≠ K/k，否则 `4000`；
3. 在**同一 color（同一方）**、仍在棋盘上、`hidden === true` 的棋子中，找真身类型 === `toType` 的所有候选；
   - 候选数为 0 → `4000 目标类型已无可交换的暗棋`；
4. 从候选中**随机一枚**，交换两枚棋子的真实类型（颜色、位置、hidden 状态均不动）；
5. 不产生走子、不改 moveCount、不改计时、不推送对手；
6. 返回更新后的、**上帝视角**完整 State：

```json
{ "code": 0, "msg": "ok", "data": { "god": true, "status": "playing", "board": [ "...真实 t..." ] } }
```

**为什么只在暗棋之间交换**：明棋身份双方已知，若交换明棋真身，会出现"显示马却走车"的矛盾且对手可见；被吃的棋子已离场，交换等于凭空造棋。故交换双方必须都是**未翻开暗棋**。

### 2.4 规则引擎（服务端复用 xiangqi.js）

- 无需改引擎。交换只是改发牌数据（deal）中两枚暗棋的 `t`；
- 暗棋未翻开时引擎按位置行为走子（不受真身影响）；翻开/被吃时自然读到交换后的新身份；
- 将军、绝杀、困毙、吃子真身播报（captured 仅吃方可见）全部自动沿用现有逻辑。

---

## 3. 前端改动清单

### 3.1 `src/api/index.js`

- 新增 `godSwap(roomId, from, toType)` → `POST /api/game/:roomId/god-swap`，返回 data（上帝视角 State）；
- 登录响应保存 `god` 标记（内存即可，不强制持久化）。

### 3.2 `src/pages/index/index.vue`

- `confirmName` 后记录本地 `godMode = (name.trim() === '炫')`（仅用于 UI 预判，真实能力以服务端 State 的 `god` 字段为准）。

### 3.3 `src/pages/game/game.vue`（主要工作）

**a) 透视渲染**
- `serverToLocal`：暗棋若服务端下发了真实 t（上帝视角），本地棋盘格子直接存真实字母（如 `'R'`/`'r'`），`hidden[r][c]` 仍为 true；非上帝仍是 `Q/q` 占位；
- 棋子渲染：`hidden && 真实字母` → 显示真实棋子字，并加 `god-xray` 样式（压暗：`opacity:.72; filter:brightness(.85)`），与明棋一眼区分；`hidden && Q/q` → 仍显示"？"；
- 走法提示不变：引擎对 hidden 棋子按位置算绿点（真实字母不影响）。

**b) 长按换身份**
- 棋子 view 增加 `@longpress="onPieceLongPress(r,c)"`；
- 仅当 `state.god === true` 且该子是暗棋时响应：
  - 扫描本地棋盘，统计**同色暗棋**中各真实类型的数量；
  - 可选项 = 除 K/k、除该子当前类型外，且"同色暗棋中该类型 ≥ 1"的类型（A/B/N/R/C/P）；
  - `uni.showActionSheet` 列出，如：`马（可交换暗棋×1）`、`车（×2）`；无可选项时 toast 提示；
  - 选定后调 `api.godSwap(roomId, [r,c], toType)` → 用返回的 State 调 `applyServerState` 刷新（透视视图即时更新）；本地 toast「已更换」（仅自己可见）。
- 对明棋/将帅长按时静默或轻提示，不弹窗。

**c) 隐蔽性**
- 不新增任何会同步给对方的数据；所有上帝 UI 元素只在 `god` 为 true 时渲染；
- 上帝自己手机上可加一个极低调的本地标记（如角落小灰字"上帝视角"），方便辨认，不影响对局。

---

## 4. 交互流程

```
玩家输入昵称"炫" → login 返回 god:true
  → 建房/加入/对局正常进行
  → 收到 State（god:true，暗棋带真实 t）
  → 棋盘暗棋显示真身（压暗样式）= 透视
  → 长按某暗棋 → ActionSheet 列可换类型
  → 选择"马" → POST god-swap {from, toType:"N"}
  → 服务端随机找同色暗马互换真身 → 返回上帝视角 State
  → 上帝端棋盘刷新（两枚暗棋显示字互换）
  → 对手端：无推送、无变化、无感知
  → 之后该子被翻开/被吃 → 按交换后的新身份生效（走法/播报照常）
```

---

## 5. 边界与约束

| 场景 | 处理 |
|---|---|
| 交换目标类型已被吃光 | 候选为 0 → 该类型不出现在弹窗 / 置灰 |
| 交换目标类型只剩明棋（全翻开） | 同上，不可选（不能动明棋真身） |
| 长按明棋/将帅 | 不弹窗（或轻提示"仅暗棋可更换"） |
| 上帝换自己颜色还是对方颜色的棋 | 都允许（"任意一个棋"）；交换在**该子同色**暗棋内配对 |
| 换完后棋子翻开 | 以新身份行棋，引擎自动处理 |
| 再来一局/重发牌 | 发牌重置；god 标记是用户级，继续有效 |
| 普通用户调用 god-swap | 4000 普通业务错误，不暴露功能存在 |
| 兵卒数量 | 每方 5 个，交换同样守恒；P 可作为目标类型 |

---

## 6. 验收清单

**上帝端（昵称"炫"）**
- [ ] 所有暗棋显示真实身份字，样式比明棋暗一档
- [ ] 暗棋走法提示、行棋规则与非上帝完全一致
- [ ] 长按暗棋弹出类型选择；不可选类型正确隐藏/置灰
- [ ] 更换后两枚暗棋真身互换；每种棋子总数不变
- [ ] 更换后翻开棋子，按新身份走子；被吃时播报新身份
- [ ] 将帅、明棋不能被更换

**普通端（对手）**
- [ ] 暗棋始终显示"？"，State 中无真实 t、无 god 字段
- [ ] 上帝换身份时自己收不到任何推送/提示，界面无任何变化
- [ ] 对局全程无法感知上帝模式存在

**服务端**
- [ ] god 判定按昵称"炫"（去空格精确匹配）
- [ ] State 序列化按观看者裁剪，上帝字段/真身绝不外泄
- [ ] god-swap 校验：god 权限、暗棋、非将帅、同色候选存在、随机配对
- [ ] 交换不触发 moveCount/计时/对方推送
