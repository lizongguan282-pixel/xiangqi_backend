# 中国象棋（揭棋）小程序 — 前端对接指南

> 本文档基于后端实际实现编写，包含每个接口的请求方式、参数、成功与失败返回值示例，供前端直接对接使用。
> 后端服务地址（API 根地址）：`https://gcwtnunyhfap.sealosbja.site`
> 开发环境：`http://localhost:8080`

**⚠️ 本次接口补充（2026-09-04，按 `api补充.md` 实现）：**

1. `POST /api/user/login` 请求新增可选 **`avatarUrl`**；响应与 `/api/user/me` 均返回 `avatarUrl`
2. **所有玩家结构均带 `avatarUrl`**：`Room.red/black`、`State.players.red/black`、以及 `room_update` / `game_start` / `move_applied` 推送（Room 带 `ready`，State.players 不带）
3. `reason` 枚举规范为 **`绝杀` / `困毙` / `认输` / `超时`** 四值（不会出现 `将死` 等），`game_over` 推送与 State 终局字段完全一致
4. `opponent_offline` 推送增加 **10 秒防抖**：断线满 10 秒未重连才推一次，重连不补发
5. WS 同一 token 可同时多端连接（前端重连幂等安全）
6. 新增**上帝模式**（见 §7）：昵称"炫"专属，State 带 `god` 字段与暗棋真身，新增 `god-swap` 接口
7. 新增**揭棋全随机模式** `mode:"random"`（见 §8）：30 枚跨颜色全盘洗牌，暗子归属/吃子/送将按"有效阵营"（暗子→位置方），战利品全公开，上帝模式强制关闭

---

## 0. 总体约定

### 0.1 统一响应结构

所有接口均返回 JSON，格式统一为：

```json
{
  "code": 0,
  "msg": "ok",
  "data": { }
}
```

- `code = 0` 表示成功，`data` 为业务数据
- `code != 0` 表示失败，`msg` 为错误描述，`data` 通常不返回

### 0.2 鉴权方式

登录成功后拿到 `token`，后续所有接口（除登录本身）在请求头携带：

```
Authorization: Bearer <token>
```

未携带或 token 失效，返回 `code: 1001`。

### 0.3 数据格式

- 请求体：`Content-Type: application/json`
- 坐标格式：`[row, col]`，如 `[9, 1]`，row 0 为黑方底线（上方），row 9 为红方底线（下方）
- 棋盘为 10 行 × 9 列

### 0.4 棋子表示

```json
null                                    // 空位
{ "t": "K", "color": "red", "hidden": false }  // 明子，t ∈ K/A/B/N/R/C/P
{ "t": "?", "color": "black", "hidden": true } // 暗子（揭棋未翻开）
```

### 0.5 错误码总表

| code | 含义 |
|------|------|
| 0 | 成功 |
| 1001 | 未登录 / token 失效 |
| 2001 | 房间不存在 |
| 2002 | 房间已满 |
| 2003 | 对局中不可加入 |
| 4000 | 参数错误 / 业务校验失败（详见各接口 msg） |
| 4001 | 不该你走棋 |
| 4002 | 非法走法 |
| 4003 | 送将 / 将帅照面 |
| 4004 | 对局已结束 |
| 4040 | 接口不存在 |
| 5000 | 服务器内部错误 |

---

## 1. 用户模块

### 1.1 微信登录

`POST /api/user/login`

**请求：**

```json
{
  "code": "wx.login()获取的code",
  "nickname": "玩家昵称",
  "avatarUrl": "https://thirdwx.qlogo.cn/.../0"
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| code | string | 是 | `wx.login()` 获取的 code |
| nickname | string | 否 | 玩家昵称 |
| avatarUrl | string | 否 | `<button open-type="chooseAvatar">` 拉到的微信头像 URL（也支持相册临时文件/CDN 地址）。**再次登录传入可更新头像**；不传或传空则保留原值 |

> 后端未配置 `WX_APPID`/`WX_SECRET` 时为开发模式，`code` 可传任意字符串（直接映射为 openid）。

**成功响应：**

```json
{
  "code": 0,
  "msg": "ok",
  "data": {
    "token": "eyJ1aWQiOiJ1XzEwMDAx...",
    "userId": "u_10001",
    "nickname": "玩家昵称",
    "avatarUrl": "https://thirdwx.qlogo.cn/.../0"
  }
}
```

> `avatarUrl` 从未设置过时返回 `null`，前端已有默认图兜底。

**失败响应：**

```json
// code 缺失
{
  "code": 4000,
  "msg": "参数错误：缺少 code"
}

// 微信 code2session 失败（生产模式）
{
  "code": 5000,
  "msg": "微信 code2session 失败: ..."
}
```

### 1.2 获取我的信息

`GET /api/user/me`

> 用于断线重连时确认登录态。

**成功响应：**

```json
{
  "code": 0,
  "msg": "ok",
  "data": {
    "userId": "u_10001",
    "nickname": "张三",
    "avatarUrl": "https://..."
  }
}
```

**失败响应：**

```json
// 未带 token 或 token 失效
{
  "code": 1001,
  "msg": "未登录或 token 失效"
}
```

---

## 2. 房间模块

房间状态机：`waiting`（等人）→ `playing`（对局中）→ `ended`（已结束）

**Room 结构：**

```json
{
  "roomId": "R5418C498",
  "mode": "flip",
  "gameTime": 600,
  "stepTime": 60,
  "status": "waiting",
  "red":   { "userId": "u_10001", "nickname": "张三", "avatarUrl": "https://...", "ready": false },
  "black": null,
  "createTime": 1730000000000
}
```

- `mode`：`flip` = 揭棋（默认）| `standard` = 标准 | `random` = 揭棋全随机（跨颜色全盘洗牌，见 §8）
- `red` / `black`：`null` 表示该位置尚无玩家
- **玩家结构**：`{ userId, nickname, avatarUrl, ready }`，其中 `avatarUrl` 从未设置过时为 `null`（前端兜底默认图）

### 2.1 创建房间

`POST /api/room/create`

**请求：**

```json
{
  "mode": "flip",
  "gameTime": 600,
  "stepTime": 60
}
```

> `mode`、`gameTime`、`stepTime` 均可选，默认 `flip` / `600` / `60`。创建者 = 红方（红先）。

**成功响应：**

```json
{
  "code": 0,
  "msg": "ok",
  "data": {
    "roomId": "R5418C498",
    "mode": "flip",
    "gameTime": 600,
    "stepTime": 60,
    "status": "waiting",
    "red": { "userId": "u_10001", "nickname": "张三", "avatarUrl": "https://...", "ready": false },
    "black": null,
    "createTime": 1788508797116
  }
}
```

### 2.2 加入房间

`POST /api/room/join`

**请求：**

```json
{ "roomId": "R5418C498" }
```

**成功响应：**

```json
{
  "code": 0,
  "msg": "ok",
  "data": {
    "roomId": "R5418C498",
    "mode": "flip",
    "gameTime": 600,
    "stepTime": 60,
    "status": "waiting",
    "red":   { "userId": "u_10001", "nickname": "张三", "avatarUrl": "https://...", "ready": false },
    "black": { "userId": "u_10002", "nickname": "李四", "avatarUrl": null, "ready": false },
    "createTime": 1788508797116
  }
}
```

**失败响应：**

```json
// 房间不存在
{ "code": 2001, "msg": "房间不存在" }

// 房间已有两人
{ "code": 2002, "msg": "房间已满" }

// 对局已开始
{ "code": 2003, "msg": "对局中不可加入" }
```

> 已在房间内重复调用 join 是幂等的，直接返回当前 Room。

### 2.3 查询房间

`GET /api/room/:roomId`

**成功响应：**

```json
{
  "code": 0,
  "msg": "ok",
  "data": { "roomId": "R5418C498", "mode": "flip", "..." : "..." }
}
```

**失败响应：**

```json
{ "code": 2001, "msg": "房间不存在" }
```

### 2.4 准备 / 取消准备

`POST /api/room/ready`

**请求：**

```json
{ "roomId": "R5418C498", "ready": true }
```

**成功响应：**

```json
{
  "code": 0,
  "msg": "ok",
  "data": { "redReady": true, "blackReady": false }
}
```

> **双方 `ready` 都为 `true` 时，服务端自动开局、发牌**，并向双方 WebSocket 推送 `game_start`（见第 4 节）。

**失败响应：**

```json
// 房间不存在
{ "code": 2001, "msg": "房间不存在" }

// 不在该房间
{ "code": 4000, "msg": "你不在该房间中" }
```

### 2.5 离开房间

`POST /api/room/leave`

**请求：**

```json
{ "roomId": "R5418C498" }
```

**成功响应：**

```json
{ "code": 0, "msg": "ok", "data": {} }
```

> 对局中离开等同于认输，会向对方广播 `game_over`。

**失败响应：**

```json
{ "code": 2001, "msg": "房间不存在" }
```

---

## 3. 对局模块

### 局面结构（State）

`GET /api/game/:roomId/state` 及 WebSocket `game_start` / `move_applied` 推送的 `data` 均为此结构：

```json
{
  "status": "playing",
  "mode": "flip",
  "turn": "red",
  "myColor": "red",
  "board": [
    [{"t":"?","color":"black","hidden":true}, null, "..."],
    "..."
  ],
  "hidden": [[false, "..."], "..."],
  "lastMove": { "from": [9,1], "to": [7,1] },
  "check": false,
  "clocks": { "red": 598, "black": 600 },
  "stepRemain": 60,
  "moveCount": 3,
  "players": {
    "red":   { "userId": "u_10001", "nickname": "张三", "avatarUrl": "https://..." },
    "black": { "userId": "u_10002", "nickname": "李四", "avatarUrl": null }
  },
  "capturedTray": {
    "red":   [{ "t": "P", "color": "black", "wasHidden": false }],
    "black": [{ "t": "?", "color": "red",  "wasHidden": true }]
  }
}
```

| 字段 | 说明 |
|------|------|
| `status` | `playing` / `ended`（未开局时为 `waiting`） |
| `turn` | 当前该谁走：`red` / `black` |
| `myColor` | 我执哪方（同一局对红黑双方返回不同值） |
| `board` | 10×9 棋盘，暗子一律 `t:"?"`，**任何一方都看不到暗子真身** |
| `hidden` | 10×9 布尔矩阵，该位置是否为未翻开的暗子 |
| `lastMove` | 上一步走子，`null` 表示尚无走子 |
| `check` | 当前行棋方是否被将军 |
| `clocks` | 双方剩余局时（秒） |
| `stepRemain` | 当前行棋方剩余步时（秒） |
| `moveCount` | 已走步数（轮询增量判断用） |
| `capturedTray` | 各方吃到的子；**自己吃的暗子见真实 `t`，对方吃的暗子 `t` 恒为 `"?"`** |
| `winner` / `reason` | 仅 `status === "ended"` 时返回；`reason` 枚举见下方 |
| `players` | `{ userId, nickname, avatarUrl }`，**不带 ready**（ready 只在 Room 结构里） |

**`reason` 枚举（前后端契约，`game_over` 推送与 State 保持完全一致）：**

| reason | 含义 | 前端表现 |
|--------|------|----------|
| `绝杀` | 将死（checkmate） | 棋盘盖红印章"绝杀" → 停留 3 秒 → 弹胜负弹窗 |
| `困毙` | stalemate | 棋盘盖红印章"困毙" → 停留 3 秒 → 弹胜负弹窗 |
| `认输` | resign | 直接宣判 |
| `超时` | clock timeout | 直接宣判 |

> 服务端保证只下发以上 4 个值（不会出现 `将死`、`checkmate` 等）；若 reason 不是 `绝杀/困毙`，前端直接弹胜负弹窗不延时。

### 3.1 获取当前局面

`GET /api/game/:roomId/state`

> 进入对局页、断线重连、轮询降级均用此接口。

**成功响应：** `data` 为上述 State 结构。

**失败响应：**

```json
{ "code": 2001, "msg": "房间不存在" }
{ "code": 4000, "msg": "你不在该房间中" }
```

### 3.2 走棋

`POST /api/game/:roomId/move`

**请求：**

```json
{ "from": [9, 1], "to": [7, 1] }
```

**成功响应：**

```json
{
  "code": 0,
  "msg": "ok",
  "data": {
    "status": "playing",
    "turn": "black",
    "board": "...",
    "moveCount": 1,
    "capturedTray": { "..." },
    "..."
  }
}
```

若此步吃掉了**暗子**，走子方（吃方）的响应额外包含 `captured` 字段（**仅吃方可见，绝不广播给被吃方**）：

```json
{
  "code": 0,
  "msg": "ok",
  "data": {
    "status": "playing",
    "capturedTray": {
      "red": [{ "t": "N", "color": "black", "wasHidden": true }]
    },
    "captured": { "t": "N", "color": "black", "revealed": true }
  }
}
```

**失败响应：**

```json
// 不是你的回合
{ "code": 4001, "msg": "不该你走棋" }

// 走法非法（坐标无效、该位置无己方棋子、不符合棋子行为）
{ "code": 4002, "msg": "非法走法" }

// 走法伪合法但走后己方被将 / 将帅照面
{ "code": 4003, "msg": "送将 / 将帅照面" }

// 对局已结束
{ "code": 4004, "msg": "对局已结束" }
```

### 3.3 认输

`POST /api/game/:roomId/resign`

**请求：** 无 body（发送 `{}` 即可）

**成功响应：**

```json
{
  "code": 0,
  "msg": "ok",
  "data": { "winner": "black", "reason": "认输" }
}
```

> 同时向双方 WebSocket 广播 `game_over`。`reason` 取值：`绝杀` / `困毙` / `认输` / `超时`。

**失败响应：**

```json
{ "code": 4004, "msg": "对局已结束" }
```

### 3.4 悔棋（协商制，可选）

**请求方发起：** `POST /api/game/:roomId/undo-request`

**请求：** 无 body（发送 `{}` 即可）

```json
{ "code": 0, "msg": "ok", "data": { "from": "black" } }
```

> 对方收到 WebSocket 推送 `undo_request`：`{ "type": "undo_request", "data": { "from": "black" } }`

**对方应答：** `POST /api/game/:roomId/undo-response`

**请求：**

```json
{ "agree": true }
```

**成功响应（同意）：**

```json
{ "code": 0, "msg": "ok", "data": { "agree": true } }
```

> 双方收到 WebSocket 推送 `undo_result`：`{ "type": "undo_result", "data": { "agree": true, "state": { State } } }`

**成功响应（拒绝）：**

```json
{ "code": 0, "msg": "ok", "data": { "agree": false } }
```

> 双方收到 `{ "type": "undo_result", "data": { "agree": false } }`

**失败响应：**

```json
{ "code": 4004, "msg": "对局已结束" }
{ "code": 4000, "msg": "没有待处理的悔棋请求" }
{ "code": 4000, "msg": "不能应答自己的悔棋请求" }
```

### 3.5 再来一局

`POST /api/game/:roomId/rematch`

**请求：** 无 body（发送 `{}` 即可）

**成功响应：**

```json
// 第一个请求方
{
  "code": 0,
  "msg": "ok",
  "data": { "started": false, "redRematch": true, "blackRematch": false }
}

// 第二个请求方（双方都请求后自动重新开局）
{
  "code": 0,
  "msg": "ok",
  "data": { "started": true, "redRematch": true, "blackRematch": true }
}
```

> 重新发牌、重置计时，状态回到 `playing`，向双方推送 `game_start`。

---

## 4. WebSocket 实时推送

### 4.1 连接

```
wss://gcwtnunyhfap.sealosbja.site/ws?token=<登录返回的token>
```

- 连接时通过 URL query 传 `token`，无效则连接被关闭
- **心跳**：客户端每 30 秒发送 `{"type":"ping"}`，服务端回复 `{"type":"pong"}`

### 4.2 服务端推送消息一览

所有消息为 JSON，通过 `type` 字段区分：

#### room_update — 房间变化

对方加入 / 离开 / 准备状态变化时推送。

```json
{ "type": "room_update", "data": { "roomId": "R5418C498", "status": "waiting", "..." : "..." } }
```

`data` 为 Room 结构（同 §2）。

#### game_start — 开局

双方就绪或再来一局时推送，`data` 为完整 State。

```json
{ "type": "game_start", "data": { "status": "playing", "myColor": "red", "..." : "..." } }
```

#### move_applied — 对手落子

对手走棋后推送，`data` 为当前视角的完整 State。

```json
{ "type": "move_applied", "data": { "status": "playing", "myColor": "black", "..." : "..." } }
```

> **对手收到的 State 中不含被吃暗子的真实身份**（该子已从棋盘移除，`capturedTray` 中暗子 `t` 恒为 `"?"`）。

#### game_over — 终局

```json
{ "type": "game_over", "data": { "winner": "red", "reason": "绝杀" } }
```

`reason` 取值：`绝杀`（将死）/ `困毙` / `认输` / `超时`。

#### undo_request — 悔棋请求

```json
{ "type": "undo_request", "data": { "from": "black" } }
```

#### undo_result — 悔棋结果

```json
// 同意
{ "type": "undo_result", "data": { "agree": true, "state": { "..." } } }

// 拒绝
{ "type": "undo_result", "data": { "agree": false } }
```

#### opponent_offline — 对手断线

```json
{ "type": "opponent_offline", "data": {} }
```

> **防抖约定**：对手 WS 全部断开后，服务端等待 **10 秒宽限期**，期满仍未重连才推送一次（且只推一次，不重复发送）；宽限期内重连成功则**不补发**。前端收到后可提示"对手离线"，并在 3 秒后拉一次 `GET state` 验证。

#### emoji — 局内快捷表情（双向）

**上行（客户端 → 服务端）：**

```json
{ "type": "emoji", "roomId": "R5418C498", "code": "e1" }
```

**下行（服务端 → 房间内双方 WS）：**

```json
{ "type": "emoji", "data": { "from": "red", "code": "e1" } }
```

| 字段 | 说明 |
|------|------|
| `roomId` | 上行必带，目标房间 |
| `code` | 表情编号，白名单 `e1`~`e30`（前端每加表情同步扩）；非法值服务端静默丢弃 |
| `from` | **服务端注入**的发送者颜色（`red`/`black`），绝不采信客户端自报 |

**规则：**
- 仅房间内 red/black 玩家可发送，旁观者/无关连接的消息被静默丢弃
- 同一房间 **500ms 限频**：限频窗口内的后续消息直接丢弃（防刷屏）
- 消息会推给**双方**（含发送者本人），前端对 `from === 自己颜色` 的回显自动跳过防重复
- 纯即时消息，不落库、不影响棋局 State；对局结束/离房后无需处理
- 表情图片在双端本地 `static/emoji/eN.png` 渲染，不涉及图片 URL 传输

### 4.3 轮询降级（不使用 WebSocket 时）

对局页每 2 秒调用 `GET /api/game/:roomId/state`：

- 比较 `moveCount`：增大 → 用返回的 State 刷新本地
- `status === "ended"` → 弹出终局弹窗

---

## 5. 典型对接流程

### 5.1 建房 → 邀请 → 对战

```
1. 红方：POST /api/user/login          → 拿到 token
2. 红方：POST /api/room/create         → 拿到 roomId
3. 红方：分享 roomId 给好友
4. 黑方：POST /api/user/login          → 拿到 token
5. 黑方：POST /api/room/join           → 加入房间
6. 双方：连接 WebSocket /ws?token=xxx
7. 红方：POST /api/room/ready {ready:true}
8. 黑方：POST /api/room/ready {ready:true}  → 服务端自动开局
9. 双方：收到 WS game_start，进入对局页
10. 轮流：POST /api/game/:roomId/move  → 对手收到 WS move_applied
11. 终局：收到 WS game_over
12. 再来一局：双方 POST /api/game/:roomId/rematch
```

### 5.2 前端本地提示 vs 服务端校验

| 前端行为 | 对接方式 |
|---------|---------|
| 选中棋子算绿点提示 | **保留本地** `pieceMoves` 计算，仅做 UI 提示 |
| 真正落子 | 改为 `POST move`，以服务端校验结果为准 |
| 将军/绝杀判定 | 依据服务端 State 的 `check` 与 `game_over` 推送 |
| 倒计时 | 显示 `clocks` / `stepRemain`，本地每秒递减做平滑 |
| 首页「开始」按钮 | 调 `POST room/ready`，监听 `room_update` 和 `game_start` |

---

## 6. 开发环境快速验证

```bash
# 1. 登录（开发模式，code 随意）
curl -X POST https://gcwtnunyhfap.sealosbja.site/api/user/login \
  -H "Content-Type: application/json" \
  -d '{"code":"test1","nickname":"红方"}'

# 2. 用返回的 token 创建房间
curl -X POST https://gcwtnunyhfap.sealosbja.site/api/room/create \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <token>" \
  -d '{"mode":"flip","gameTime":600,"stepTime":60}'

# 3. 健康检查
curl https://gcwtnunyhfap.sealosbja.site/health
```

---

## 7. 上帝模式（昵称"炫"专属）

> 触发条件：登录时昵称去空格后 === `"炫"`。仅该玩家可见的调试/娱乐功能，对手完全无感知。

### 7.1 god 标记

`POST /api/user/login` 与 `GET /api/user/me` 的响应 `data` 增加：

```json
{ "token": "...", "userId": "u_10001", "nickname": "炫", "avatarUrl": "...", "god": true }
```

- 非上帝用户 `god: false`；再次登录换昵称可覆盖该标记

### 7.2 上帝视角 State

上帝玩家收到的所有 State（`GET state`、WS `game_start` / `move_applied` / `god-swap` 响应）：

- 顶层额外带 `"god": true`（非上帝无此字段，用于显式开启上帝 UI）
- **暗棋 `t` 下发真实类型**（大写 `K/A/B/N/R/C/P`），但 `hidden` 仍为 `true` —— 透视渲染用
- 对手收到的 State 逐字节不变（`t:"?"`、无 god 字段）

### 7.3 上帝换身份

`POST /api/game/:roomId/god-swap`

**请求：**

```json
{ "from": [7, 2], "toType": "N" }
```

| 字段 | 说明 |
|------|------|
| from | 要更换的暗棋坐标 `[row, col]`（自己或对方的暗棋均可） |
| toType | 目标类型，大写 `A/B/N/R/C/P`（不含 K） |

**规则：**

- "更换"本质是**同色两枚未翻开暗棋交换真身**（总数守恒），服务端随机挑一枚候选
- 将帅（K/k）、已翻开明棋、已被吃棋子不可更换；目标类型无同色存活暗棋 → 4000
- 不产生走子、不改 `moveCount`/计时、**不向对手推送任何消息**

**成功响应：** `data` 为上帝视角完整 State（换完后两枚暗棋显示字互换）。

**失败响应：** 一律 `4000`（普通业务错误，不暴露该功能存在）：

```json
{ "code": 4000, "msg": "无权限" }                          // 非上帝调用
{ "code": 4000, "msg": "仅未翻开的暗棋可更换" }            // 明棋
{ "code": 4000, "msg": "将帅不可更换" }                    // K/k
{ "code": 4000, "msg": "目标类型已无可交换的暗棋" }        // 候选为 0
{ "code": 4000, "msg": "参数错误：toType 无效" }           // 含 toType:"K"
```

### 7.4 前端对接要点

- `state.god === true` 时才渲染上帝 UI（透视 + 长按换身份），其余设备无任何差异
- 透视：`hidden && t!=='?'` → 显示真实棋子字并压暗（如 `opacity:.72; filter:brightness(.85)`）
- 长按暗棋 → 统计同色暗棋各类型数量 → ActionSheet 列可换类型（排除 K、排除当前类型、数量为 0 的类型）→ 调 `god-swap` → 用返回 State 刷新
- 换身份后翻开/被吃按新身份生效，走法提示（引擎按位置算）不受影响

---

## 8. 揭棋全随机模式 `mode:"random"`

> 与 `flip` 的核心差异：30 枚非将帅棋子**不分颜色全盘洗牌**；暗子归属/吃子/送将按**有效阵营**（暗子→位置方，明子→真身颜色）；战利品全公开；上帝模式强制关闭；**翻棋送将立即判负**（翻出敌子直接攻击己方将帅 → `reason:"绝杀"`，见 §8.6）。

### 8.1 建房

`POST /api/room/create` 传 `{"mode":"random"}`，Room/join/ready/leave 字段全部不变。

### 8.2 State 下发（random 保密升级）

- 所有暗子 `t:"?"`，且 **`color` 掩码为位置方颜色**：row≥5（红半场）→ `"red"`，row≤4（黑半场）→ `"black"`；真身颜色绝不下发
- **不携带 `god` 字段**（即使昵称"炫"）
- 双方收到的棋盘逐字节一致

### 8.3 走法规则（有效阵营）

| 行为项 | 判定依据 |
|--------|----------|
| 走法类型 | 位置标准类型（`standardTypeAt`，与真身无关） |
| 兵/卒前进方向 | 位置方：红半场向 row 减小，黑半场向 row 增大 |
| 士九宫 / 相河界 | 位置方 |
| **归属 / 吃子 / 送将** | **有效阵营**：暗子→位置方，明子→真身颜色 |
| 翻开后 | 按真身 + 真身颜色行棋（归属切换） |
| 跨半场暗子 | 可互吃；同半场暗子不可互吃 |

> 送将保护：走子子的有效阵营一方的将（红半场暗子被红方走时只保护红帅）。

### 8.4 战利品全公开（random）

- flip 模式：对方盘上暗子捕获显示 `"?"`
- **random 模式：双方 `capturedTray` 均下发真实 `t` + `color`**，同类型叠加角标计数
- 战利品一律记入"走子方"盘子（与被吃子真身颜色无关）
- move 响应的 `captured`（吃暗子提示）照旧仅吃方收到

### 8.5 上帝模式关闭

- State 无 `god` 字段，暗子全 `?`
- `POST /api/game/:roomId/god-swap` → `4000 该模式不支持`
- 同用户进 flip 模式上帝功能不受影响

### 8.6 翻棋送将（翻出敌子直接攻击己方将帅 → 立即判负）

全随机模式下，走子翻开的真身可能是**对方的棋**（当场易主）。若这枚敌子在其落点直接攻击行棋方**自己的将帅**，构成"翻棋送将"——对方下一手即可吃将帅，行棋方无任何应对机会，必败：

- 服务端在**走子并翻开真身后**，于真实棋盘上检查行棋方将帅是否被将（`isInCheck(realBoard, moverColor, hidden, flipMode)`）。掩码盘校验发现不了这种情况——掩码盘上它是己方 Q/q，真盘上它是敌子
- 被将 → **立即终局**：走子响应的 State 直接 `status:"ended"`、`winner = 对方`、`reason = "绝杀"`，并向双方推送 `game_over`（无需等对方真的走一手吃将）
- 前端**无需也无法预判**（客户端不知道暗子真身），按既有终局流程：先渲染最后一步落子/翻开 → "绝杀"印章 → 3 秒 → 胜负弹窗
- 配套：走子后的 `check` 字段与将死/困毙判定同样必须基于**真实棋盘**——翻开的敌子归对方所有、对方可以走它，`hasLegalMove` 必须把它计入对方着法，否则会误判困毙
- 分界线：**落子前合法性校验用掩码盘（防泄密），落子后终局/将军判定用真盘**
- flip 模式真身颜色恒等于位置方，翻出的恒为己方棋子，该规则天然不触发；standard 无翻棋
